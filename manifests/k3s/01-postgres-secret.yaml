apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: transcript-pipeline
type: Opaque
stringData:
  # IMPORTANT: Update these values before applying to your cluster
  # For production, consider using external secret management (Sealed Secrets, External Secrets Operator)
  POSTGRES_USER: "transcript_user"
  POSTGRES_DB: "transcripts_db"
  POSTGRES_PASSWORD: "xA6N9TCVuwFKq8c@*m"  # Must be changed before deployment
